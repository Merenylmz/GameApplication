import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GameOverScreens = () => {
    return (
        <View>
            <Text>Game over screens</Text>
        </View>
    );
};

export default GameOverScreens;

const styles = StyleSheet.create({});
